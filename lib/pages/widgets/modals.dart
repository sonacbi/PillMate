// lib/pages/widgets/modals.dart
import 'package:flutter/material.dart';

class BottomModals {
  // 이 파일은 ManagePage 내에서 직접 모달 UI를 쓰기 때문에
  // 별도 위젯 반환이 필요하다면 여기 유틸 함수를 추가할 수 있습니다.
  // 하지만 현재 ManagePage에서 직접 빌드하므로 빈 채로 둡니다.
}
